# API base URL
# Use relative path behind reverse proxy:
REACT_APP_BASE_URL=/api/v1

# Or set absolute URL if frontend and backend are on different domains:
# REACT_APP_BASE_URL=https://api.your-domain.com/api/v1
